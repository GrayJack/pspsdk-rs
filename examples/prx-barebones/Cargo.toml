[package]
name = "psp-prx-barebones"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = "s"

[profile.release]
opt-level = "s"
lto = "thin"
# panic = "abort"

[dependencies]
pspsdk = { path = "../../" }
