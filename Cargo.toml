# cargo-features = ["panic-immediate-abort"]

[workspace]
members = ["pspsdk-macros", "pspsdk", "examples/*"]
exclude = ["cargo-pspbuild"]
resolver = "2"

[profile.dev]
opt-level = "s"

[profile.release]
opt-level = "s"
lto = "thin"
# panic = "abort"
# panic = "immediate-abort"
